nav.nav  
    -   var items = ['Home', 'About Us', 'Our Approach', 'Our Work']
        each item in items 
            if item === 'Home'
                a.nav__link.nav__link_active(href="#")= item
            else 
                a.nav__link(href="#")= item